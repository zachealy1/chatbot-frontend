{% from "govuk/components/header/macro.njk" import govukHeader %}
{% from "govuk/components/service-navigation/macro.njk" import govukServiceNavigation %}
{% from "govuk/components/input/macro.njk" import govukInput %}
{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "govuk/components/date-input/macro.njk" import govukDateInput %}

{% extends "template.njk" %}

{% block pageTitle %}
  HMCTS chatbot
{% endblock %}

{% block header %}
  {{ govukHeader({
    classes: "govuk-header--full-width-border"
  }) }}

  {{ govukServiceNavigation({
    serviceName: "HMCTS chatbot",
    navigation: [
      {
        href: "/chat",
        text: "Chat"
      },
      {
        href: "/chat-history",
        text: "Chat History"
      },
      {
        href: "/contact-support",
        text: "Contact Support"
      },
      {
        href: "/account",
        text: "Account",
        active: true
      },
      {
        href: "/logout",
        text: "Logout",
        attributes: {
        id: "logoutLink"
      }
      }
    ]
  }) }}
{% endblock %}

{% block content %}
  <h1 class="govuk-heading-xl">Manage Your Account</h1>

  <!-- Success Alert -->
  {% if updated %}
    <div class="govuk-notification-banner govuk-notification-banner--success" role="alert" aria-labelledby="govuk-notification-banner-title" data-module="govuk-notification-banner">
      <div class="govuk-notification-banner__header">
        <h2 class="govuk-notification-banner__title" id="govuk-notification-banner-title">
          Success
        </h2>
      </div>
      <div class="govuk-notification-banner__content">
        <h3 class="govuk-notification-banner__heading">
          Your account has been updated successfully
        </h3>
        <p class="govuk-body">Contact <a class="govuk-notification-banner__link" href="#">example@department.gov.uk</a> if you think there’s a problem.</p>
      </div>
    </div>
  {% endif %}

  <!-- Server-side error summary -->
  {% if errors %}
    <div class="govuk-error-summary" role="alert" tabindex="-1">
      <h2 class="govuk-error-summary__title">There is a problem</h2>
      <ul class="govuk-list govuk-error-summary__list">
        {% for error in errors %}
          <li>{{ error }}</li>
        {% endfor %}
      </ul>
    </div>
  {% endif %}

  <form action="/account/update" method="POST" novalidate>
    <!-- Username Section -->
    <div class="govuk-form-group">
      <h1 class="govuk-label-wrapper">
        <label class="govuk-label--l govuk-label" for="username">Username</label>
      </h1>
      <input
        class="govuk-input govuk-!-width-one-half {% if errors.username %}govuk-input--error{% endif %}"
        id="username"
        name="username"
        type="text"
        placeholder="Enter your username"
        value="{{ username | default('example_user') }}"
        required
        aria-describedby="{% if errors.username %}username-error{% endif %}"
      >
      {% if errors.username %}
        <span id="username-error" class="govuk-error-message">
          <span class="govuk-visually-hidden">Error:</span> {{ errors.username }}
        </span>
      {% endif %}
    </div>

    <!-- Email Section -->
    <div class="govuk-form-group">
      <h1 class="govuk-label-wrapper">
        <label class="govuk-label--l govuk-label" for="email">Email</label>
      </h1>
      <input
        class="govuk-input govuk-!-width-one-half {% if errors.email %}govuk-input--error{% endif %}"
        id="email"
        name="email"
        type="email"
        placeholder="Enter your email address"
        value="{{ email | default('example@example.com') }}"
        required
        aria-describedby="{% if errors.email %}email-error{% endif %}"
      >
      {% if errors.email %}
        <span id="email-error" class="govuk-error-message">
          <span class="govuk-visually-hidden">Error:</span> {{ errors.email }}
        </span>
      {% endif %}
    </div>

    <!-- Date of Birth Section -->
    {% set dateInputParams = {
      fieldset: {
        legend: {
          text: "Date of birth",
          isPageHeading: true,
          classes: "govuk-fieldset__legend--l"
        }
      },
      hint: {
        text: "For example, 27 3 1985"
      },
      id: "date-of-birth",
      namePrefix: "date-of-birth",
      items: [
        {
          classes: "govuk-input--width-2 {% if errors.dateOfBirth %}govuk-input--error{% endif %}",
          name: "day",
          value: day | default('21')
        },
        {
          classes: "govuk-input--width-2 {% if errors.dateOfBirth %}govuk-input--error{% endif %}",
          name: "month",
          value: month | default('5')
        },
        {
          classes: "govuk-input--width-4 {% if errors.dateOfBirth %}govuk-input--error{% endif %}",
          name: "year",
          value: year | default('1985')
        }
      ]
    } %}

    {% if errors.dateOfBirth %}
      {% set dateInputParams = dateInputParams | merge({
        errorMessage: {
          text: errors.dateOfBirth
        }
      }) %}
    {% endif %}

    {{ govukDateInput(dateInputParams) }}

    <!-- Password Section -->
    <div class="govuk-form-group" id="password-section">
      <h1 class="govuk-label-wrapper">
        <label class="govuk-label--l govuk-label">Password</label>
      </h1>
      <div id="password-hint" class="govuk-hint">
        <p class="govuk-body">Your password must:</p>
        <ul class="govuk-list govuk-list--bullet">
          <li>Be at least 8 characters long</li>
          <li>Include an uppercase letter</li>
          <li>Include a lowercase letter</li>
          <li>Include a number</li>
          <li>Include a special character (e.g., @, $, !, %, *, ?)</li>
        </ul>
      </div>

      <!-- New Password -->
      <label class="govuk-label" for="password">New Password</label>
      <input
        class="govuk-input govuk-input-password govuk-!-width-one-half {% if errors.password %}govuk-input--error{% endif %}"
        id="password"
        name="password"
        type="password"
        placeholder="Leave blank to keep the current password"
        aria-describedby="password-hint {% if errors.password %}password-error{% endif %}"
      >
      {% if errors.password %}
        <span id="password-error" class="govuk-error-message">
          <span class="govuk-visually-hidden">Error:</span> {{ errors.password }}
        </span>
      {% endif %}
      <button
        type="button"
        class="govuk-button govuk-button--secondary govuk-button-password--secondary toggle-button"
        data-target="password"
        data-module="govuk-button"
        aria-expanded="false">
        Show
      </button>

      <!-- Confirm New Password -->
      <label class="govuk-label" for="confirm-password">Confirm New Password</label>
      <input
        class="govuk-input govuk-!-width-one-half {% if errors.confirmPassword %}govuk-input--error{% endif %}"
        id="confirm-password"
        name="confirmPassword"
        type="password"
        placeholder="Re-enter the new password"
        aria-describedby="{% if errors.confirmPassword %}confirm-password-error{% endif %}"
      >
      {% if errors.confirmPassword %}
        <span id="confirm-password-error" class="govuk-error-message">
          <span class="govuk-visually-hidden">Error:</span> {{ errors.confirmPassword }}
        </span>
      {% endif %}
      <button
        type="button"
        class="govuk-button govuk-button--secondary toggle-button"
        data-target="confirm-password"
        data-module="govuk-button"
        aria-expanded="false">
        Show
      </button>
    </div>

    <!-- Submit Button -->
    {{ govukButton({
      text: "Save Changes",
      classes: "govuk-!-margin-top-3",
      type: "submit"
    }) }}
  </form>

  <script src="showPassword-dev.js"></script>
  <script src="passwordValidationAccount-dev.js"></script>
  <script src="dateOfBirthValidation-dev.js"></script>
  <script src="emailValidation-dev.js"></script>
  <script src="logout-dev.js"></script>
{% endblock %}
